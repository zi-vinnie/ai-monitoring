AGENT_URL=http://192.168.1.50:8000
AGENT_API_KEY=changeme
SCREENSHOT_DIR=data/screenshots
DB_PATH=data/metadata.sqlite3
# Optional: your LAN gateway/router IP. If set, an unreachable agent is pinged
# against it to tell "Wi-Fi/router down" apart from "machine off". Leave blank
# to skip that probe.
ROUTER_IP=192.168.1.1
